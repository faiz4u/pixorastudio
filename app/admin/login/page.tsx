/**
 * Placeholder admin login page. Replaced with the real Supabase email/password
 * form (and session redirect handling) in the auth/admin-shell phase.
 */
export default function AdminLoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6">
      <p className="text-sm text-muted-foreground">Admin login — coming soon.</p>
    </main>
  );
}
