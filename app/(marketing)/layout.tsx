/**
 * Layout for every public marketing page (home, portfolio, etc). Header and
 * footer components will be added here in the phase that builds out the
 * public site sections.
 */
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
